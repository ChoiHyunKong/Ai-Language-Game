/**
 * 게임 엔진 - 스텁 버전
 */

const gameEngine = {
  initialize() {
    console.log('게임 엔진 초기화');
  },

  startGame(config, session) {
    console.log('게임 시작:', config);
  }
};

export default gameEngine;
